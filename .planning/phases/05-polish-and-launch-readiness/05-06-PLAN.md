---
phase: 05-polish-and-launch-readiness
plan: 06
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
  - "05-04"
  - "05-05"
autonomous: false
files_modified: []
requirements:
  - SECR-03
  - SECR-04

must_haves:
  truths:
    - "SECR-04 (event creation rate limiting) is confirmed working in the existing codebase"
    - "All five 'done when' criteria from the Phase 5 goal are verified by a human"
    - "The app passes a smoke test on iOS Safari covering the full user journey"
  artifacts:
    - path: "src/lib/rate-limit.ts"
      provides: "eventCreationRatelimit (SECR-04 — already implemented)"
      contains: "eventCreationRatelimit"
    - path: "src/app/api/events/route.ts"
      provides: "Rate limit applied before DB work"
      contains: "eventCreationRatelimit"
  key_links:
    - from: "src/lib/rate-limit.ts"
      to: "src/app/api/events/route.ts"
      via: "import { eventCreationRatelimit }"
      pattern: "eventCreationRatelimit\\.limit"
---

<objective>
Final verification: confirm SECR-04 is already implemented and working, then run a complete human smoke test covering all five Phase 5 "done when" criteria including cross-browser and keyboard-only navigation.

Purpose: No new code — this plan verifies all Phase 5 work is complete and the app is ready for strangers to use.
Output: Confirmed launch readiness.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish-and-launch-readiness/05-01-SUMMARY.md
@.planning/phases/05-polish-and-launch-readiness/05-02-SUMMARY.md
@.planning/phases/05-polish-and-launch-readiness/05-03-SUMMARY.md
@.planning/phases/05-polish-and-launch-readiness/05-04-SUMMARY.md
@.planning/phases/05-polish-and-launch-readiness/05-05-SUMMARY.md
@src/lib/rate-limit.ts
@src/app/api/events/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SECR-04 (event creation rate limiting already implemented)</name>
  <files></files>
  <action>
SECR-04 was implemented in Phase 1 Plan 02. Verify it is still correct and functioning:

1. Read `src/lib/rate-limit.ts` and confirm `eventCreationRatelimit` is defined with `Ratelimit.slidingWindow(10, '1 m')`.

2. Read `src/app/api/events/route.ts` and confirm:
   - `eventCreationRatelimit` is imported from `@/lib/rate-limit`.
   - `eventCreationRatelimit.limit(ip)` is called as step 2 (before JSON parsing and DB operations).
   - The 429 response is returned with `Retry-After` header when rate limit is exceeded.

3. Run a quick manual test from the command line. Ensure the dev server is running. Send 11 rapid requests and confirm the 11th gets a 429:
   ```bash
   for i in $(seq 1 12); do
     STATUS=$(curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3000/api/events \
       -H "Content-Type: application/json" \
       -d '{"title":"test","dateMode":"specific_dates","specificDates":"[\"2026-03-01\"]","dayStart":9,"dayEnd":17,"timezone":"UTC"}')
     echo "Request $i: $STATUS"
   done
   ```
   Expect: first ~10 return 201 or 422 (Zod may fail on minimal body), subsequent ones return 429.

   NOTE: In local dev environment without Upstash Redis configured, this test may return 200/201 for all requests (rate limiting is disabled locally without UPSTASH credentials). If UPSTASH env vars are present in .env.local, the test will work correctly. Either way, confirm the code wiring is correct by code inspection.

4. Produce a summary confirming SECR-04 is implemented and wired correctly. No code changes are needed.
  </action>
  <verify>
- `eventCreationRatelimit` is defined in src/lib/rate-limit.ts with slidingWindow(10, '1 m').
- The limit is applied in src/app/api/events/route.ts before any DB operations.
- Rate limit exceeded returns 429 with Retry-After header.
  </verify>
  <done>SECR-04 confirmed implemented — no new code required. Evidence: code inspection of rate-limit.ts and events/route.ts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke test — all Phase 5 done-when criteria</name>
  <files></files>
  <action>Human verification — follow the how-to-verify steps to confirm all Phase 5 done-when criteria.</action>
  <verify>All 24 verification steps completed and approved by human reviewer.</verify>
  <done>Human types "approved" confirming all five Phase 5 done-when criteria pass.</done>
  <what-built>
All Phase 5 workstreams completed across Plans 01–05:
- SECR-03: Vercel Cron job (vercel.json + /api/cron/expire-events)
- Creator delete: /e/[id]/manage page, deleteEvent Server Action, DeleteEventButton, FlashToast, "Manage event" link on event page
- Honeypot: sr-only field in create-event-form, fake 201 in API route
- Privacy: inline note below submit, /privacy page with structured sections, footer on all pages
- Empty/error states: /e/[id]/not-found.tsx, /app/not-found.tsx, expiry check in event page
- Accessibility: ARIA grid roles on heatmap, focus-visible rings on interactive elements, keyboard support on BestTimeCallout
  </what-built>
  <how-to-verify>
Verify all five Phase 5 "done when" criteria:

**Criterion 1 — Auto-expiry (SECR-03)**
1. Confirm `vercel.json` exists at the project root with `"schedule": "0 3 * * *"`.
2. Visit `http://localhost:3000/api/cron/expire-events` with no Authorization header — confirm it returns "Unauthorized".
3. With CRON_SECRET in .env.local, run: `curl -s -H "Authorization: Bearer $(grep CRON_SECRET .env.local | cut -d= -f2)" http://localhost:3000/api/cron/expire-events` — confirm JSON response with `deleted` count.

**Criterion 2 — Rate limiting (SECR-04)**
4. Confirm `eventCreationRatelimit.limit(ip)` appears in `src/app/api/events/route.ts` before the DB insert.

**Criterion 3 — Expired event page**
5. Visit `http://localhost:3000/e/somefakeid` — confirm the event not-found page renders ("This event isn't available") rather than a 500 or blank screen.
6. Optional: temporarily set `expiresAt` on a real event to a past date in the DB and confirm the event page shows the not-found UI.

**Criterion 4 — Keyboard navigation**
7. Open `http://localhost:3000` (home page).
8. Press Tab repeatedly — confirm focus ring (orange) appears on the form inputs, date picker buttons, date mode toggle buttons, and the Create event submit button.
9. Open an event page. Tab through the "Mark my availability" and "Already joined?" buttons — confirm orange focus rings appear.
10. If you are the event creator and there are responses: Tab to the BestTimeCallout card — confirm it receives focus (orange ring) and pressing Enter opens the ConfirmTimeSheet.
11. Check the heatmap grid: Tab into the grid area and confirm at least one cell in each row receives focus.

**Criterion 5 — iOS Safari smoke test**
12. On an iPhone running iOS Safari, navigate to the app URL.
13. Create a new event: fill in title, select 2-3 specific dates, set time window, submit.
14. Share the event link. Open it (or open in a new Safari tab).
15. Tap "Mark my availability". Enter a name and 4-digit PIN. Complete the join flow.
16. In the availability grid: drag across multiple time slots in one gesture. Verify cells paint without page scroll.
17. Tap "Save availability".
18. Return to the event page. Verify the heatmap shows your availability.
19. As creator: tap the BestTimeCallout. Confirm the ConfirmTimeSheet slides up. Tap "Confirm this time".
20. Verify the event page now shows "Meeting confirmed" banner.

**Additional spot checks**
21. Visit `http://localhost:3000/privacy` — confirm the page renders with all sections (data collected, retention, deletion, no accounts, contact).
22. Check the footer on any page — confirm "Privacy" link is visible.
23. On the home page, check below the Create event button — confirm the privacy note: "Events and all responses expire automatically after 30 days. No accounts required."
24. Navigate to `/e/[your-event-id]/manage` as creator — confirm delete button appears and the AlertDialog shows "Delete this event? / This cannot be undone."
  </how-to-verify>
  <resume-signal>Type "approved" if all criteria pass, or describe any issues found with specific test numbers.</resume-signal>
</task>

</tasks>

<verification>
All five Phase 5 "done when" criteria verified by human:
1. Auto-expiry cron wired (vercel.json + route handler, 401 without auth)
2. Rate limiting confirmed implemented (code inspection)
3. Expired/missing event shows not-found page (not 500)
4. Keyboard navigation works with visible focus rings
5. iOS Safari full flow: create → join → grid → heatmap → confirm
</verification>

<success_criteria>
Human approves all 5 criteria. Phase 5 is complete. The app is ready for strangers to use.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-launch-readiness/05-06-SUMMARY.md` following the summary template.
</output>
